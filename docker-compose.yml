version: '3.8'
services:
  redis-queue:
    image: redis:7-alpine
    container_name: nest-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data: